{
  "name": "crypto-lab-x3dh-wire",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
